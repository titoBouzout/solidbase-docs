---
title: For
---

# For

The `<For>` component provides efficient list rendering in Solid.

## Usage

```tsx
import { For } from 'solid-js';

function TodoList(props: { todos: Todo[] }) {
  return (
    <For each={props.todos}>
      {(todo) => <div>{todo.text}</div>}
    </For>
  );
}
```
